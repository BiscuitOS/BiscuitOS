#
# Host toolchain configure
#
menu "Host toolchain"

config HOST_MKFS_MINIX
	bool "Build minix fs from Host tools"
	help
	  Build a minix fs with mkfs.minix that create from host tools.

config HOST_DEBUG
	bool "Debug BiscuitOS on HOST"
	help
	  Utilse different tool to debbug BiscuitOS on HOST.

if HOST_DEBUG

source target/Host/qemu/Kconfig

endif

endmenu

menuconfig PK_GNU_BISON
	bool "GNU - bison"

if PK_GNU_BISON

config GNU_BISON_VERSION
	default "3.2"
	string "verstion"

config GNU_BISON_TAR
	default "tar.gz"
	string "tar type"

config GNU_BISON_URL
	default "http://ftp.gnu.org/pub/gnu/bison/"
	string "url"

config GNU_BISON_CONFIG
	string "configure"

endif # PK_GNU_BISON

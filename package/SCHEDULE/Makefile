BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_APP_USER) 			+= BiscuitOS-SCHED-APP-USER-MODE/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_APP_KERNEL) 		+= BiscuitOS-SCHED-APP-KERNEL-MODE/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_KTHREAD) 			+= BiscuitOS-SCHED-KTHREAD/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_SOFTIRQ) 			+= BiscuitOS-SCHED-SOFTIRQ/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_INTERRUPT) 		+= BiscuitOS-SCHED-INTERRUPT/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_WAITQUEUE) 		+= BiscuitOS-SCHED-WAITQUEUE/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_WORKQUEUE) 		+= BiscuitOS-SCHED-WORKQUEUE/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_TASKLET) 			+= BiscuitOS-SCHED-TASKLET/
BiscuitOS-SCHEDULE-$(CONFIG_PK_SCHED_COMPLETE) 			+= BiscuitOS-SCHED-COMPLETE/

PACKAGE_MAKEDIR := $(patsubst %/, $(srctree)/package/SCHEDULE/%/Makefile, $(BiscuitOS-SCHEDULE-y))

include $(PACKAGE_MAKEDIR)

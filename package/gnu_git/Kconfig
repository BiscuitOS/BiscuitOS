menuconfig PK_GNU_GIT
	bool "GNU - git"

if PK_GNU_GIT

config GNU_GIT_VERSION
	default "4.9.5"
	string "verstion"

config GNU_GIT_TAR
	default "tar.gz"
	string "tar type"

config GNU_GIT_URL
	default "http://ftp.gnu.org/pub/gnu/git/"
	string "url"

config GNU_GIT_LDFLAGS
	string "LDFLAGS"

config GNU_GIT_CFLAGS
	string "CFLAGS"

config GNU_GIT_CONFIG
	string "configure"

endif # PK_GNU_GIT

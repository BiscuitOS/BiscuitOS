menuconfig PK_FS
	bool "Filesystem"

if PK_FS

# BiscuitOS Filesystem
source "package/filesystem/bsvfs/Kconfig"

# Kernfs
source "package/filesystem/kernfs/Kconfig"

# sysfs
source "package/filesystem/sysfs/Kconfig"

# vfs
source "package/filesystem/vfs/Kconfig"

endif # PK_FS

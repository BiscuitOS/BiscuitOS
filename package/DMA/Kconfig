menuconfig PK_DMA
	bool "DMA: Coherent Memory Allocate"

if PK_DMA

# DMA Memory mmap into Userspace (Userspace Stub)
source package/DMA/BiscuitOS-DMA-MMAP-TO-USERSPACE/Kconfig

# DMA Memory mmap into Userspace (Kernel Stub)
source package/DMA/BiscuitOS-DMA-MMAP-TO-USERSPACE-K/Kconfig

# DMABUF
source package/DMA/BiscuitOS-DMA-DMABUF/Kconfig

endif # PK_DMA

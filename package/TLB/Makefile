TLB-$(CONFIG_PK_TLB_FLUSH_MPROTECT)		+= BiscuitOS-TLB-FLUSH-MPROTECT/
TLB-$(CONFIG_PK_TLB_FLUSH_MADVISE)		+= BiscuitOS-TLB-FLUSH-MADVISE/
TLB-$(CONFIG_PK_TLB_FLUSH)			+= BiscuitOS-TLB-FLUSH/
TLB-$(CONFIG_PK_TLB_FLUSH_APP)			+= BiscuitOS-TLB-FLUSH-APP/
TLB-$(CONFIG_PK_TLB_CPUID)			+= BiscuitOS-TLB-CPUID/
TLB-$(CONFIG_PK_TLB_FLUSH_ALL)			+= BiscuitOS-TLB-FLUSH-ALL/
TLB-$(CONFIG_PK_TLB_FLUSH_NOTIFY)		+= BiscuitOS-TLB-FLUSH-NOTIFY/
TLB-$(CONFIG_PK_TLB_FLUSH_MADVFREE)		+= BiscuitOS-TLB-FLUSH-MADV-FREE/
TLB-$(CONFIG_PK_TLB_FLUSH_MADVDONTNEED)		+= BiscuitOS-TLB-FLUSH-MADV-DONTNEED/
TLB-$(CONFIG_PK_TLB_FLUSH_MADVPAGEOUT)		+= BiscuitOS-TLB-FLUSH-MADV-PAGEOUT/
TLB-$(CONFIG_PK_TLB_FLUSH_MADVCOLD)		+= BiscuitOS-TLB-FLUSH-MADV-COLD/
TLB-$(CONFIG_PK_TLB_SHOOTDOWN)			+= BiscuitOS-TLB-SHOOTDOWN/
TLB-$(CONFIG_PK_TLB_FLUSH_MDPTE)		+= BiscuitOS-TLB-FLUSH-MODIFY-PTE/
TLB-$(CONFIG_PK_TLB_FLUSH_MDPTE_APP)		+= BiscuitOS-TLB-FLUSH-MODIFY-PTE-APP/

PACKAGE_MAKEDIR := $(patsubst %/, $(srctree)/package/TLB/%/Makefile, $(TLB-y))

include $(PACKAGE_MAKEDIR)

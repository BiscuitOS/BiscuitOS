#
# Package common configure
#
menu "Common utilise"

config COMMON_UTILISE
	bool "user toolchain"
	help
	  binutilise

if COMMON_UTILISE

source package/demo/Kconfig

source package/objdump/Kconfig

source package/gcc/Kconfig

source package/utils-linux/Kconfig

endif

endmenu
